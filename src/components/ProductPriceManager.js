//상품 가격 수정 컴포넌트
import React, { useState, useEffect } from "react";
import styled from "styled-components";
import cafe24ProductService from "../services/ProductService.js";

const Container = styled.div`
  padding: 20px;
  border: 1px solid #ddd;
  border-radius: 8px;
  margin-bottom: 20px;
`;

const ProductPriceManager = ({ isAuthenticated }) => {
  return <></>;
};

export default ProductPriceManager;
